import AdminDashboard from "@/components/AdminDashboard";

export const metadata = {
  title: "Admin Responses | Yolla & Pras",
};

export default function AdminPage() {
  return <AdminDashboard />;
}
